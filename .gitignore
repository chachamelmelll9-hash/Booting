# See https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files for more about ignoring files.

# compiled output
dist
tmp
out-tsc

# dependencies
node_modules

# IDEs and editors
/.idea
.project
.classpath
.c9/
*.launch
.settings/
*.sublime-workspace

# IDE - VSCode
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json

# Environment variables
.env
.env.local
.env.development
.env.production
.env.*.local
!.env.example

# misc
/.sass-cache
/connect.lock
/coverage
/libpeerconnection.log
npm-debug.log
yarn-error.log
testem.log
/typings

# System Files
.DS_Store
Thumbs.db

# Turborepo
.turbo

# Native (generated by expo prebuild)
apps/mobile/ios/
apps/mobile/android/

# Expo
node_modules/
.expo/
.yarn/
dist/
npm-debug.*
*.jks
*.p8
*.p12
*.key
*.pem
*.mobileprovision
*.orig.*
web-build/
cache/


test-output

vite.config.*.timestamp*
vitest.config.*.timestamp*

# Next.js
.next
out

.mcp.json
supabase/.temp/
.codex/state/
.claude/state/
__pycache__/

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# Signing & store credentials (never commit)
keystore.properties
apps/mobile/keystore.properties
apps/mobile/release.jks
google-service-account.json
.appstoreconnect.env
# 심사용 데모 계정 비밀번호 (/preflight 생성, /launch 소비)
.launch-demo-account

# Store build artifacts
*.aab
*.ipa
apps/mobile/build-*.aab
# release APK (deploy Phase 4 프로덕션 스모크 검증용)
apps/mobile/build-*.apk

# TypeScript incremental build info
apps/webview/tsconfig.tsbuildinfo

# Oracle provisioning local state (provision-oracle.sh)
infra/oracle/.deploy-state

# E2E / device test logs (mobile-e2e, adb-verify)
test-results/

# auto mode supervisor 런타임 산출물 (Stop 훅 auto-commit 이 대용량 로그를 커밋하지 않도록)
docs/progress/runs/
docs/progress/rate-limit.json
docs/progress/supervisor.json
