---
type: Decision
title: 부모님 동의는 카톡 링크에서 부모님이 직접 누른다
description: 자녀가 '여쭤봤습니다'를 누르는 대면/문자 선택을 없애고, 서버가 그린 동의 페이지 링크를 보내 부모님이 직접 누른 기록만 동의로 인정한다. 문안은 개인정보보호법 고지 항목대로.
tags: [decision, consent, legal]
sources:
  - id: caae19f6
    resource: commit:aae19f6
  - id: cdfb7e40
    resource: commit:dfb7e40
generated:
  by: claude-code/claude-fable-5-1
  at: 2026-09-11T14:30:00+09:00
status: stable
---

# 동의는 링크로

## 배경
TODO-04 확정값은 "자녀 단말 대면 동의 + 성함·시각 기록". 소유자(09-03): "부모님 동의는 어떻게 동의 받으셨나요? 이거 없애고 부모님 동의 받기 누르면 카톡으로 동의 링크 나라가게끔! 법적기준 적용해서 동의 내용은 작성해줘", "완성되면 카톡으로 직접 테스트해줘".

## 결정
- 선택지(대면/문자) 제거. `[부모님 동의 받기]` → 링크 생성(32바이트 토큰, **3일**) → 공유 시트로 발송 → 부모님이 `GET /consent/:token` 에서 `[동의합니다]` → `consented_at`·IP·UA·`CONSENT_VERSION` 기록. 자녀 화면은 보내고 기다리기만.
- 서버가 HTML 을 직접 그린다 — 부모님은 앱이 없고, 페이지 하나 때문에 웹앱을 띄우고 카카오 콘솔에 도메인을 더 등록할 이유가 없다.
- 문안: 목적·항목·기간 각각(제15조 2항), 제3자 제공(제17조 2항), 거부권·불이익, 열람·정정·삭제·처리정지·철회(제35~37조, 제39조의7). 큰 글자, 짧은 문장. **문안 수정 시 `CONSENT_VERSION` 올림.**
- 카드가 아니라 **일반 메시지**로 (`dfb7e40`) — 카드 링크는 콘솔 등록 도메인만 열리고(`INTENT_NOT_RESOLVED`), 카톡 안 쓰는 부모님께는 문자가 낫다. 시트를 닫으면 기다림을 시작하지 않는다.
- 곁들여: 생년월일 입력 형식 안내(placeholder 는 한 글자 치면 사라진다).

## 근거
"자녀가 '직접 여쭤봤습니다' 를 눌러 스스로 기록했다. 그건 자녀의 진술이지 부모님의 동의가 아니다. … 다투게 되면 동의를 받았다는 사실을 증명해야 하는 쪽은 우리다."

## 검증
카톡 안에서 끝까지: 공유 시트 → 링크 도착 → 인앱 브라우저 동의서 → 동의 → DB(`agreed_user_agent: KAKAOTALK/26.7.2 (INAPP)`) → 자녀 화면 전환.

## 이후
개발 빌드 '부모님이 동의하신 것으로 처리' 는 같은 `agree` 를 부른다 (`2a78a18`). 부모님 웹 결정 버튼도 "법적 동의도 이미 같은 방식의 링크로 받고 있어 새로 생기는 위험이 아니다" 를 근거로 삼았다. 관련: [parent-consent](../concepts/parent-consent.md).
