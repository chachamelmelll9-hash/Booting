---
type: Source
title: 세션 2026-09-07~09 — 출시 준비, 부모님은 웹으로, 호스팅
description: "이제 진짜 앱 배포할거야" 에서 시작해 실제 앨범·릴리스 빌드·부모님 웹 전환·본인인증 실물화·AWS/운영 DB 준비까지. 사주 논의로 끝난다.
tags: [source, session]
resource: session:beebb230-c0d5-44e2-8b3c-4b26fcc4e1ca
sources:
  - id: s
    resource: session:beebb230-c0d5-44e2-8b3c-4b26fcc4e1ca
generated:
  by: claude-code/claude-fable-5-1
  at: 2026-09-11T14:30:00+09:00
status: stable
---

# 세션 beebb230 (09-07 09:05 → 09-09 11:34 KST)

사용자 메시지 90, 어시스턴트 343, 도구 호출 611. 컴팩션 없음 — 전문이 그대로 남아 있다.

## 09-07 — 두 대 띄우기, 실제 앨범

- "booting 부모화면, 자녀화면 각각 애뮬레이터로 띄워줘" → 2대(5554 자녀 / 5556 부모). 두 창이 `y=-661` 에 겹쳐 있어 화면 밖 — Win32 `MoveWindow` 로 내림.
- "엥 내가 어제 추천프로필이랑 받은 프로필 다 카드 뒤집는걸로 바꿔달라고햇는데??? 왜 반영안됏어?" → 로컬이 origin 뒤에 있었다(`52a1f90`). "fetch 없이 로컬만 보고 '어제 작업 없음' 이라 한 것은 틀렸다" 고 정정. `git pull --ff-only`, Metro·서버 재기동. 마이그레이션 `20260906120000_expire_received_hearts` PENDING → 적용은 소유자가(`! node scripts/db-migrate.mjs`).
- "매일 6장씩 리프레쉬되는건 로직반영된거지?" → 로컬 자정 기준·스냅샷·전부 클라이언트라는 한계 설명 — [daily-picks](../concepts/daily-picks.md).
- 세 번째 에뮬레이터(신규 가입 → 등록 5/5). "부모님 동의처리실패 라는데??" → `PUBLIC_BASE_URL` 이 **이전 세션의 터널 주소**. `dev-up.ps1` 이 주소를 출력만 하고 env 에 안 쓴다 — 재발 경로.
- "부모님 사진 등록할떄 실제 앨범에 접근하게 고쳐줘 이제 진짜 앱 배포할거야" → `expo-image-picker` `e477b74`. 권한 검토: Android 12 이하는 `WRITE_EXTERNAL_STORAGE` 가 필요해 차단을 되돌림. arm64 릴리스 빌드에서 **한글 사용자명 Gradle 캐시 경로 mojibake** → Gradle 홈 ASCII 복사(5.21GB) `5a8ed05`. "응 스크립트에 넣고 릴리스 빌드로 arm64까지 확인해줘" → AAB 64.8MB (첫 성공).
- Metro 가 자꾸 죽는 진짜 이유: `getNativeModuleVersionsAsync` 가 응답 본문을 두 번 읽음(`Body is unusable`) → 의존성 검증 끄기 — [metro-and-dev-servers](../lessons/metro-and-dev-servers.md).
- 17:46 "실제 배포하면 본인인증이랑 부모님 동의(카카오톡공유) 프로필 공유 … 다 작동되는거지? 그리구 궁금한데 부모님은 앱 다운안받아도되는거야?" → "딥링크 버튼 먼저 고쳐줘 그리고 그럼 부모님이 앱없이 자녀가 보낸 링크 클릭하면 앱을 다운받아주세요 화면이 뜨는거야? 나는 그거 기획한적이없는데" — PRD 의 부모님 접점은 동의 하나뿐임을 확인.

## 09-08 — 웹으로, 본인인증

- 10:46 "b로 가줘 본인인증 스텁도 해주고 완성하면 부모화면 웹도 보여줘" → **부모님 웹** `51850a2`, 거절 시 목록 유지 `605267c`.
- "일단 본인인증 시작해줘 부모암ㄹ호는 생각좀해볼게" → 실물 인증 `3995f52`. 마이그레이션이 유니크 인덱스에서 실패(스텁 시절 `01000000000` 에 14계정) → "근데 초기화하면 화면테스팅을못하잔아" / "이건 배포직전에 하는게 낫진않아? 아직 고칠게많아" → 인덱스를 배포 직전 SQL 로 분리 `e944130`.
- "흠 혹시 카카오로그인만 가능하게하면 문자인증 이거 생략해도되나" / "사업자는 없고....... 아모르겟어 그냥 본인확인할수잇게해줘 카카오로그인으로" → **카카오 연결 = 계정 확인** `46a5735`, 문구 '계정을 한 번 확인합니다', 문자 항목 감춤 `45900cf`.
- "아니 내 카카오에서 로그인동의 눌럿는데 다음으로 안넘어가잔아~~!!!!!! 샹" → 실기기 카카오 로그인 확인 후 "기존 계정 로그인 확인 그럼 이제 남은 질문은 부모이신가요? 랑 개발용 회원가입은 없애야하는거지" → "그럼 앱이랑 웹 둘다잇는거야 지금 부모화면?" → "응 앱 부모화면 지워줘 서버는 두고" `eac0e7c`.

## 09-09 — 호스팅, 운영 DB, 사주

- "고정 도메인 어떻게 만드는지 알려줘" / "오라클 가입이안돼.... 근데 고정 도메인이 없으면 어케돼?" / "아 그럼 한번쓴 주소는 몇분이상 접속안하면 죽는겨?" / "vercel은왜안댐" — 터널은 PC 프로세스에 묶여 있음, Render 는 15분 잠듦, **Vercel 은 `setInterval` 유지보수 루프·Dockerfile·콜드스타트·Hobby 비상업 때문에 부적합**, Oracle 은 카드/리전/용량으로 자주 실패.
- "aws로해줘" / "무료로하고싶어" → EC2 + 업체 중립 워크플로 `798b4e1`. 소유자가 시드니 리전으로 가입 → 서울로. 계정이 **크레딧 $100/182일**임을 확인 → "12개월 무료" 정정, Elastic IP 도 유료 정정, `t4g.micro` 권고. "일단 지금 ec2 런치안하면 6개월지나도 유료안되는건가" → 안 켜면 과금 없음, 기간은 흐름 → **EC2 는 준비 후에**.
- "응 env.production부터 정리하자" → `.env.example` 누락 3개(`PUBLIC_BASE_URL`, `KAKAO_NATIVE_APP_KEY`, `CORS_ORIGINS`) `8d2cf5e`.
- 개발 DB 에 시드 38계정 → "B로 가줘 새 프로젝트 만들어서 옮겨줘" → **`booting-prod` 생성**. 마이그레이션 18개 PENDING — 적용은 소유자에게 요청(`SUPABASE_PROJECT_REF=vyiauclgelpuxitsoeii`). 카카오 로그인은 Supabase provider 설정에 의존하지 않는다고 확인. **DB 비밀번호가 대화에 그대로 찍혔다** → [open-questions](../open-questions.md).
- 11:28 "사주기반 매칭기능을 넣고싶은데 할수잇겟어?" → 그릇(`saju_infos`)은 있으나 입력 화면·계산이 없음. 규칙은 제품 결정이라 임의로 정하면 안 됨. 선택지 A(직접 규칙) B(외부 API) C(정보만) → "B오떄" → SAZU 등 검토, **생년월일을 외부에 보내면 제3자 제공 동의가 필요**함을 지적 → 세션 종료. 다음 세션에서 서버 직접 계산으로 결론 — [결정](../decisions/2026-09-09-saju-free-in-mvp.md).

## 정정 기록 (이 세션에서 어시스턴트가 스스로 고친 것)

- "어제 작업 흔적이 없다" → fetch 전이었다
- "EC2 12개월 무료" → 크레딧 방식
- "Elastic IP 는 붙어 있으면 무료" → 공인 IPv4 시간당 과금
- "사장님" 호칭 → 소유자가 개발자임을 혼동시켜 사과 후 중단
