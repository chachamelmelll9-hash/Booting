{
  "name": "@chachamelmelll9-hash-service/webview",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "eslint src",
    "typecheck": "tsc -p tsconfig.app.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@chachamelmelll9-hash-service/i18n": "workspace:*",
    "@chachamelmelll9-hash-service/webview-bridge": "workspace:*"
  }
}
